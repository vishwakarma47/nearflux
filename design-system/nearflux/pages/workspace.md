# NearFlux Workspace Override — Deep Signal

The workspace page overrides the previous neutral theme with a dark mission-control aesthetic. Use `#070B14` as the page background, `#0F1824` for panels, `#162032` for raised/hover states, `#00C2FF` as the signal accent, `#00E5A0` for connected/success status, `#F0F6FF` for primary text, `#5A7090` for muted text, and a subtle white 6% border.

Use Space Grotesk for the NearFlux wordmark and display/headline typography, Inter for body/UI text, and JetBrains Mono for room codes and technical identifiers. The main transfer hero is the signature element: a dark glass card with a centered animated cyan pulse-ring/sonar visualization that accelerates and glows more strongly during drag-over. Use a 64px icon rail with hover tooltips, a room code treatment with cyan glow, a frosted QR frame, a subtle pill-like copy link, glass device/activity cards, pulsing online indicators, and timeline/log-style activity.

Keep interactions within 150–300ms, avoid excessive decoration, use Lucide icons consistently, retain visible focus states, preserve `prefers-reduced-motion`, and ensure 44px+ controls on touch layouts. The visual language should feel like a beam or signal, not a generic file manager.
