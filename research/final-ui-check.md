# Final bundle UI check

The final compiled client title is `NearFlux Online — Private Direct P2P File Sharing`. The room dialog opens from the room section and displays the active code, local share URL, QR code, close-room and regenerate-code controls, copy-link control, and a join-code field.

A test code `FLUX-ABCD` was entered into the join field successfully. Submission was not performed in this check; room switching is already covered by the server integration test and code path.
