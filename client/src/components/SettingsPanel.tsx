import { Check, Cookie, KeyRound, UserRound } from 'lucide-react';
import { useApp } from '../context/AppContext';

export const SettingsPanel: React.FC = () => {
  const { theme, toggleTheme, deviceName } = useApp();
  return <div className="settings-grid"><section className="workspace-card settings-card"><div className="panel-title"><span className="panel-icon"><UserRound size={16} aria-hidden="true" /></span><div><span className="section-kicker">PROFILE</span><h2>Guest profile</h2></div></div><p className="settings-copy">You are using NearFlux as a guest. Your device identity stays in this browser and no account is required for direct transfers.</p><div className="settings-row"><span><strong>Device</strong><small>{deviceName}</small></span><span className="settings-status"><Check size={13} aria-hidden="true" /> Active</span></div><button className="btn secondary account-btn" type="button" disabled><KeyRound size={15} aria-hidden="true" /> Add an account <span className="coming-soon">Coming soon</span></button></section><section className="workspace-card settings-card"><div className="panel-title"><span className="panel-icon transfer-panel-icon"><Cookie size={16} aria-hidden="true" /></span><div><span className="section-kicker">BROWSER STORAGE</span><h2>Guest history</h2></div></div><p className="settings-copy">Transfer history is saved locally in this browser so it remains available after you return, even without signing in.</p><div className="settings-row"><span><strong>Theme</strong><small>{theme === 'dark' ? 'Deep Signal dark' : 'Light mode'}</small></span><button className="btn secondary compact-btn" type="button" onClick={toggleTheme}>Switch theme</button></div></section></div>;
};
